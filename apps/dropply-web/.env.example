# Dropply web -> API base URL.
# Dev: leave empty — nsl serves web+api same-origin at http://dropply.localhost:3355 (relative /api).
# Prod: the dropply-api Worker URL, e.g. https://dropply-api.<subdomain>.workers.dev
NEXT_PUBLIC_API_URL=
